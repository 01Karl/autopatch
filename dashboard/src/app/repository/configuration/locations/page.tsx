import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Configuration Locations',
};

export { default } from '@/app/configuration/locations/page';
