import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Configuration Settings',
};

export { default } from '@/app/configuration/settings/page';
