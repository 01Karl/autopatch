import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Configuration Organizations',
};

export { default } from '@/app/configuration/organizations/page';
