import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Provisioning Hardware Models',
};

export { default } from '@/app/provisioning/hardware-models/page';
