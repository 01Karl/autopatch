import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Configure Playbooks',
};

export { default } from '@/app/configure/playbooks/page';
