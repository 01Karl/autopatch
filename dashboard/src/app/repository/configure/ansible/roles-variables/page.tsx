import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Configure Roles & Variables',
};

export { default } from '@/app/configure/ansible/roles-variables/page';
