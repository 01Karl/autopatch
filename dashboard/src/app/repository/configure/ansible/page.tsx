import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Configure Ansible',
};

export { default } from '@/app/configure/ansible/page';
