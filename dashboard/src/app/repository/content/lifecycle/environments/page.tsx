import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Content Lifecycle Environments',
};

export { default } from '@/app/content/lifecycle/environments/page';
