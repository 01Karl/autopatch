import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Content Product Credentials',
};

export { default } from '@/app/content/product-credentials/page';
