import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Overseer | Repository Management | Content Synchronization',
};

export { default } from '@/app/content/synchronization/page';
